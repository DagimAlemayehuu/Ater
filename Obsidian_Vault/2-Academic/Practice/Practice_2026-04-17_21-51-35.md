---
type: practice
hub_id: 3_Relational_Model_And_Database_Design_Hub.md
date: '2026-04-17'
difficulty: L3
question_types:
- multipleChoice
- trueFalse
- shortAnswer
- scenario
- codeImplementation
- clozeDeletion
- findTheError
- matchingMatrix
config:
  hubId: 3_Relational_Model_And_Database_Design_Hub.md
  selectedAtomicNotes: []
  exclusionKeywords: []
  timeBoundDays: null
  prioritizeWeaknesses: false
  questionDistribution:
    multipleChoice: 1
    trueFalse: 1
    shortAnswer: 1
    scenario: 1
    codeImplementation: 1
    clozeDeletion: 1
    findTheError: 1
    matchingMatrix: 1
  difficulty: L3
  distractorPlausibility: High
  injectTrickAnswers: false
  globalTimeLimitMinutes: 5
  perQuestionTimeLimitSeconds: null
  progressionGatekeeper: false
  gradingStrictness: Flexible
  enableProgressiveHints: false
  requireConfidenceWager: false
score: null
completed: false
---
# 3 Relational Model And Database Design Hub - L3 Session

### Q1 [mcq]: What is the primary purpose of an Entity-Relationship (ER) diagram in database design?
- **A)** To define the physical storage of data
- **B)** To specify the database management system
- **C)** To represent the logical structure of a database
- **D)** To implement data security

***

### Q2 [true_false]: A weak entity type has a primary key that is partially or totally derived from another entity type.

***

### Q3 [short_answer]: What is the difference between a strong entity type and a weak entity type?

***

### Q4 [scenario]: Suppose we have a database for a university with entities 'Student', 'Course', and 'Enrollment'. The 'Enrollment' entity has attributes 'enrollment_date' and 'grade'. Is 'Enrollment' a strong or weak entity type?

***

### Q5 [code]: Given the following code snippet in SQL, identify the correct implementation of a one-to-many relationship between 'Department' and 'Employee':
```
```sql
CREATE TABLE Department (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(255)
);

CREATE TABLE Employee (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(255),
    dept_id INT
);
```

***

### Q6 [cloze]: The [[ ]] model is used to represent the structure of a database, while the [[ ]] model is used to represent the relationships between entities.

***

### Q7 [find_error]: Given the following buggy code snippet in SQL, identify the error and provide the correct implementation:

***

### Q8 [matching]: Match the following terms with their definitions:

***

## Session Data
```json
[
  {
    "id": 1,
    "type": "mcq",
    "difficulty": "L3",
    "question": "What is the primary purpose of an Entity-Relationship (ER) diagram in database design?",
    "explanation": "An ER diagram is used to represent the logical structure of a database, including entities, attributes, and relationships. It helps designers to visualize and communicate the design of a database.",
    "hints": [],
    "options": {
      "A": "To define the physical storage of data",
      "B": "To specify the database management system",
      "C": "To represent the logical structure of a database",
      "D": "To implement data security"
    },
    "answer": "C"
  },
  {
    "id": 2,
    "type": "true_false",
    "difficulty": "L3",
    "question": "A weak entity type has a primary key that is partially or totally derived from another entity type.",
    "explanation": "A weak entity type does not have a primary key of its own and relies on another entity type for its existence. This is in contrast to a strong entity type, which has a primary key.",
    "hints": [],
    "answer": "true"
  },
  {
    "id": 3,
    "type": "short_answer",
    "difficulty": "L3",
    "question": "What is the difference between a strong entity type and a weak entity type?",
    "explanation": "A strong entity type has a primary key and can exist independently, while a weak entity type does not have a primary key and relies on another entity type for its existence.",
    "hints": [],
    "answer": "A strong entity type has a primary key and can exist independently, while a weak entity type does not have a primary key and relies on another entity type for its existence."
  },
  {
    "id": 4,
    "type": "scenario",
    "difficulty": "L3",
    "question": "Suppose we have a database for a university with entities 'Student', 'Course', and 'Enrollment'. The 'Enrollment' entity has attributes 'enrollment_date' and 'grade'. Is 'Enrollment' a strong or weak entity type?",
    "explanation": "Enrollment is a weak entity type because it relies on both 'Student' and 'Course' for its existence. It cannot exist independently without these entities.",
    "hints": [],
    "answer": "Enrollment is a weak entity type."
  },
  {
    "id": 5,
    "type": "code",
    "difficulty": "L3",
    "question": "Given the following code snippet in SQL, identify the correct implementation of a one-to-many relationship between 'Department' and 'Employee':",
    "explanation": "To implement a one-to-many relationship, we need to add a foreign key in the 'Employee' table that references the primary key of the 'Department' table.",
    "hints": [],
    "codeSnippet": "```sql\nCREATE TABLE Department (\n    dept_id INT PRIMARY KEY,\n    dept_name VARCHAR(255)\n);\n\nCREATE TABLE Employee (\n    emp_id INT PRIMARY KEY,\n    emp_name VARCHAR(255),\n    dept_id INT\n);",
    "answer": "```sql\nCREATE TABLE Department (\n    dept_id INT PRIMARY KEY,\n    dept_name VARCHAR(255)\n);\n\nCREATE TABLE Employee (\n    emp_id INT PRIMARY KEY,\n    emp_name VARCHAR(255),\n    dept_id INT,\n    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n);",
    "language": "SQL"
  },
  {
    "id": 6,
    "type": "cloze",
    "difficulty": "L3",
    "question": "The [[ ]] model is used to represent the structure of a database, while the [[ ]] model is used to represent the relationships between entities.",
    "explanation": "The Entity-Relationship (ER) model is used to represent the structure of a database, while the relational model is used to represent the relationships between entities.",
    "hints": [],
    "textWithBlanks": "The [[ ]] model is used to represent the structure of a database, while the [[ ]] model is used to represent the relationships between entities.",
    "answer": [
      "Entity-Relationship",
      "relational"
    ]
  },
  {
    "id": 7,
    "type": "find_error",
    "difficulty": "L3",
    "question": "Given the following buggy code snippet in SQL, identify the error and provide the correct implementation:",
    "explanation": "The error is in the foreign key constraint of the 'Employee' table. It should reference the 'dept_id' of the 'Department' table, not the 'dept_id' of the 'Employee' table.",
    "hints": [],
    "buggyCode": "```sql\nCREATE TABLE Department (\n    dept_id INT PRIMARY KEY,\n    dept_name VARCHAR(255)\n);\n\nCREATE TABLE Employee (\n    emp_id INT PRIMARY KEY,\n    emp_name VARCHAR(255),\n    dept_id INT,\n    FOREIGN KEY (dept_id) REFERENCES Employee(dept_id)\n);",
    "answer": "```sql\nCREATE TABLE Department (\n    dept_id INT PRIMARY KEY,\n    dept_name VARCHAR(255)\n);\n\nCREATE TABLE Employee (\n    emp_id INT PRIMARY KEY,\n    emp_name VARCHAR(255),\n    dept_id INT,\n    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n);"
  },
  {
    "id": 8,
    "type": "matching",
    "difficulty": "L3",
    "question": "Match the following terms with their definitions:",
    "explanation": "The correct matches are: Entity - A thing or concept that has existence and can be described, Attribute - A characteristic or property of an entity, Relationship - An association between two or more entities, Primary Key - A unique identifier for a tuple in a relation.",
    "hints": [],
    "pairs": [
      {
        "left": "Entity",
        "right": "A thing or concept that has existence and can be described"
      },
      {
        "left": "Attribute",
        "right": "A characteristic or property of an entity"
      },
      {
        "left": "Relationship",
        "right": "An association between two or more entities"
      },
      {
        "left": "Primary Key",
        "right": "A unique identifier for a tuple in a relation"
      }
    ]
  }
]
```
