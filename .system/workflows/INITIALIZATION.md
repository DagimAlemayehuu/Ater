# INITIALIZATION RUNBOOK
> **ATTENTION AGENT:** Run this first on every session.

1.  Read `AGENTS.md`.
2.  Read `.system/state/STATE.md`.
3.  Read `.system/prompts/prompt.md`.
4.  Run `pnpm install` and `cd apps/api && uv sync`.
5.  Check sidecar health with `pnpm dev:all`.
